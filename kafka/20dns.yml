# A headless service to create DNS records
---
apiVersion: v1
kind: Service
metadata:
  name: broker
  namespace: jolt-dev
spec:
  ports:
  - port: 9092
  # [podname].broker.jolt.svc.cluster.local
  clusterIP: None
  selector:
    app: kafka
